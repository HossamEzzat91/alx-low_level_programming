#include <stdio.h>

/**
 * main - Entry point
 * @argc:no of args
 * @argv: value of args
 * Return: Always 0 (Success)
 */
int main(int argc, char **argv)
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
