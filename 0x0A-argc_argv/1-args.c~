#include <stdio.h>

/**
 * main - Entry point
 * @argc:no of args
 * @argv: value of args
 * Return: Always 0 (Success)
 */
int main(int argc, char **argv__attribute__((unused)))
{
printf("%d\n", argc - 1);
return (0);
}
