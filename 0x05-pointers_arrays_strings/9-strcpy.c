#include "main.h"
/**
 * _strcpy- function that prints every other character of a string
 * @dest: pointer of type char
 * @src : pointer to src string
 * Return: char
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
