#include"main.h"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
long double y = 1;
int x = 1;
long double prev = 0;
long double temp = 0;
while (x <= 98)
{
temp = y;
y = y + prev;
prev = temp;
printf("%lf", y);
x++;
if (x <= 98)
{
printf(", ");
}
}
printf("\n");
return (0);
}
